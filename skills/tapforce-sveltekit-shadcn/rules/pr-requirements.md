---
name: pr-requirements
description: Shadcn UI requirements for project
tags:
    - requirements
    - prepare
---

## Requirements

- Project MUST use pnpm as main package manager.
- Project MUST use SvelteKit as main framework.
- Project MUST use Svelte version >= 5.
- Project MUST use tailwindcss as main css framework. The version must >= 4.